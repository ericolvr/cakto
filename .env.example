# ========================================
# INSTRUÇÕES DE USO:
# ========================================
# 1. Copie este arquivo para .env (nunca versione .env no Git!)
# 2. Escolha o ambiente:
#    - Desenvolvimento LOCAL (Django fora do Docker): use .env.local como base
#    - Docker/Produção (tudo containerizado): use .env.docker como base
# 3. Ajuste SECRET_KEY e senhas conforme necessário
# ========================================

# Django Settings
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database PostgreSQL
# LOCAL: DB_HOST=localhost
# DOCKER: DB_HOST=postgres
DB_NAME=cakto
DB_USER=cakto
DB_PASSWORD=cakto
DB_HOST=localhost
DB_PORT=5432

# Django Database URL (alternativa)
DATABASE_URL=postgresql://cakto:cakto@localhost:5432/cakto

# RabbitMQ
# LOCAL: CELERY_BROKER_URL=amqp://cakto:cakto@localhost:5672//
# DOCKER: CELERY_BROKER_URL=amqp://cakto:cakto@rabbitmq:5672//
RABBITMQ_DEFAULT_USER=cakto
RABBITMQ_DEFAULT_PASS=cakto
CELERY_BROKER_URL=amqp://cakto:cakto@localhost:5672//
